name: Publish to PyPI

on:
  workflow_dispatch:
env:
  UV_FROZEN: "1"

permissions:
  contents: read

jobs:
  build-package:
    if: startsWith(github.ref, 'refs/tags')
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10']
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - uses: ./.github/actions/setup-python-uv
      - name: Build the distribution package
        run: |
          uv build
      - name: Upload the distribution
        uses: actions/upload-artifact@v4
        with:
          name: distribution-artifacts
          path: dist
          if-no-files-found: error
  publish-package:
    if: startsWith(github.ref, 'refs/tags')
    runs-on: ubuntu-latest
    needs:
      build-package
    environment:
      name: pypi
      url: https://pypi.org/project/agent-lifecycle-toolkit
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download the distribution package
        uses: actions/download-artifact@v4
        with:
          name: distribution-artifacts
          path: dist/
      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
          print-hash: true
          verbose: true
